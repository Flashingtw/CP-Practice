#include <bits/stdc++.h>using namespace std;int main(){	string s;	cin>>s;	vector<string> r;	sort(s.begin(),s.end());	do{		r.push_back(s);	}while(next_permutation(s.begin(),s.end()));	cout << r.size() << "\n";	for(string n : r){		cout << n << "\n";	}}